# Pin

PostgreSQL-native, Rust-first full-text search.

This repository is being initialized for G0: a buildable, auditable PostgreSQL 18 host boundary. It does not yet implement a searchable index or claim benchmark results.
